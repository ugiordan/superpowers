# Test Results

Run the integration tests to verify skill structure:

```bash
./skills/reflective-implementation/tests/test-integration.sh
```

Tests verify: file existence, non-empty content, markdown headers, frontmatter, no @ link references, and structural completeness.
